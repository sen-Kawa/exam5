#ifndef WARLOCK_HPP
#define WARLOCK_HPP

#include <ios>

class Warlock
{
	private:

	public:
		Warlock();
		~Warlock();
		Warlock(Warlock const& obj);
		Warlock& operator=(Warlock const& rhs);
};

#endif
